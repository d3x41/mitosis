import { types as t } from "@marko/compiler";
import { doc, Doc, ParserOptions } from "prettier";
export default function withBlockIfNeeded(node: t.Statement, opts: ParserOptions, docs: Doc): doc.builders.Doc;
