"use strict";
exports.__esModule = true;
var isMitosisPath_1 = require("../isMitosisPath");
describe('is mitosis file helper', function () {
    it('should return true if file name contains "lite" in the extension', function () {
        var path = 'component.lite.tsx';
        var result = (0, isMitosisPath_1["default"])(path);
        expect(result).toBe(true);
    });
    it('should return true if file name doesn\'t contain "lite" in the extension', function () {
        var path = 'component.tsx';
        var result = (0, isMitosisPath_1["default"])(path);
        expect(result).toBe(false);
    });
    it('should return false if file name contains "lite" in the filename itself', function () {
        var path = 'lite.anything.tsx';
        var result = (0, isMitosisPath_1["default"])(path);
        expect(result).toBe(false);
    });
});
